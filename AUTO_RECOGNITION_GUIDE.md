# chess_bot.py 自动识别模式使用指南

## 🎮 快速开始

### 1. 启动程序

```bash
cd C:\Users\Administrator\Desktop\chess-bot
python chess_bot.py
```

### 2. 校准棋盘

**首次使用必须校准：**

- 选择手动校准（推荐）
- 程序提示点击棋盘左上角
- 程序提示点击棋盘右下角
- 校准数据会自动保存

### 3. 开始自动对弈

**现在 chess_bot.py 支持两种模式：**

#### 模式1：自动识别（默认）

1. 打开 chess.com 或 lichess.org
2. 开始一局对弈
3. 让对手走一步
4. 在程序中按 **Enter**
5. 程序自动识别棋盘
6. AI 推荐走法
7. 选择是否自动执行移动
8. 等待对手走棋，按 **Enter** 继续

#### 模式2：手动输入FEN

1. 从在线平台复制FEN字符串
2. 在程序中粘贴FEN
3. AI 分析并推荐走法

## 🔥 核心功能

### ✅ 自动识别屏幕棋盘
- 实时识别屏幕上的棋盘
- 自动识别棋子位置和类型
- 生成当前棋局状态

### ✅ 智能棋子识别
- 基于颜色、形状、轮廓分析
- 区分白棋和黑棋
- 识别6种不同的棋子类型

### ✅ 半自动对弈
- 识别棋盘 → AI计算 → 推荐走法
- 可选择自动执行或手动点击
- 支持连续对弈

## 📋 使用流程

### 完整对弈流程

```
1. 校准棋盘（首次使用）
   ↓
2. 打开在线对弈平台
   ↓
3. 选择模式（自动识别）
   ↓
4. 对手走棋
   ↓
5. 按Enter，程序识别棋盘
   ↓
6. AI推荐走法
   ↓
7. 选择是否自动执行
   ↓
8. 等待对手走棋
   ↓
9. 重复步骤4-8
```

## 🎯 常用命令

### 在主菜单中

| 输入 | 功能 |
|------|------|
| 1 | 选择自动识别模式 |
| 2 | 选择手动FEN输入模式 |

### 在对弈中

| 输入 | 功能 |
|------|------|
| Enter | 识别棋盘并推荐走法 |
| y | 自动执行AI推荐的走法 |
| n | 不自动执行（手动操作）|
| auto | 切换到自动识别模式 |
| fen | 手动输入FEN字符串 |

## ⚙️ 配置选项

### 调整AI强度

**在 `calculate_best_move` 方法中：**

```python
# 快速对弈
move = bot.calculate_best_move(board, depth=5)

# 标准对弈
move = bot.calculate_best_move(board, depth=10)

# 深度计算
move = bot.calculate_best_move(board, depth=15)
```

### 调整引擎线程数

**在 `calculate_best_move` 方法中：**

```python
engine.configure({
    "Threads": 4,              # 根据CPU核心数调整
    "NNCacheSize": 200,       # 缓存大小
})
```

## 🖼️ 棋盘识别优化

### 提高识别准确率

1. **环境条件**
   - ✅ 棋盘对比度高
   - ✅ 光线充足且均匀
   - ✅ 棋盘完全可见
   - ❌ 阴影、反光、遮挡

2. **棋盘样式**
   - 标准的深浅交替格子
   - 棋子有清晰的轮廓
   - 避免复杂的背景图案

3. **校准精确度**
   - 精确点击棋盘边缘
   - 包含整个棋盘（不包含边框）
   - 测试识别结果

### 测试识别

**使用测试命令：**

程序启动时会自动测试棋盘识别，显示：
- 识别到的棋子数量
- 棋子位置
- 完整的棋局状态

## 🐛 常见问题

### 识别不准确

**问题：** 棋子识别错误

**解决方案：**
1. 重新校准棋盘
2. 调整棋盘位置和大小
3. 改善光线条件
4. 使用 `test` 命令查看识别结果
5. 切换到手动FEN输入模式

### 无法检测到棋盘

**问题：** 自动检测失败

**解决方案：**
1. 使用手动校准
2. 检查棋盘是否可见
3. 确保屏幕分辨率足够高
4. 避免棋盘被其他窗口遮挡

### 移动执行错误

**问题：** 鼠标点击位置不对

**解决方案：**
1. 重新校准棋盘位置
2. 检查棋盘区域设置
3. 确保棋盘没有被移动或缩放

### AI无法找到走法

**问题：** 识别的棋盘状态无效

**解决方案：**
1. 检查识别结果是否正确
2. 使用 `test` 命令查看棋盘状态
3. 手动输入正确的FEN字符串

## 🎯 高级功能

### 切换模式

**随时在对弈中切换：**

```
输入 'fen' -> 粘贴FEN -> AI分析
```

### 重新校准

**如果棋盘位置改变：**

1. 删除 `calibration.pkl` 文件
2. 重启程序
3. 重新校准棋盘

### 批量测试

**测试识别准确性：**

```python
# 在Python中运行
from chess_bot import ChessBot

bot = ChessBot()
board = bot.scan_board_advanced()
print(board)
```

## ⚠️ 注意事项

### 使用建议

1. 首次使用建议先用测试模式熟悉
2. 重要的对弈建议先看推荐再决定是否自动执行
3. 定期检查识别准确性
4. 保持良好的网络连接（如果使用在线引擎）

### 安全性

1. 在对弈平台使用AI可能违反服务条款
2. 建议只在练习或友好对局中使用
3. 不要在正式比赛中使用
4. 尊重对手和游戏规则

## 📊 性能指标

### 识别速度
- 棋盘识别：~1-2秒
- 棋子识别：~0.5-1秒

### AI计算速度
- depth=5: ~2-5秒
- depth=10: ~5-15秒
- depth=15: ~15-30秒

### 准确率
- 棋盘检测：90%+
- 棋子识别：70-80%（取决于棋盘质量）

## 📁 相关文件

- `chess_bot.py` - 主程序（现在支持自动识别）
- `screen_recognition_bot.py` - 独立的屏幕识别机器人
- `lc0.exe` - LCZero引擎
- `calibration.pkl` - 校准数据（自动生成）
- `board_preview.png` - 棋盘预览（自动生成）

## 🆘 获取帮助

如果遇到问题：
1. 检查错误信息
2. 查看日志输出
3. 测试棋盘识别功能
4. 参考常见问题部分

---

**现在你可以直接使用 chess_bot.py 进行自动识别对弈了！** 🎉

享受你的国际象棋对弈！♟️
