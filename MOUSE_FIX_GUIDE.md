# 鼠标点击问题故障排除指南

## 🔍 问题：AI推荐走法但没有实际执行

### 可能的原因

#### 1. 棋盘校准不准确
**症状：** AI推荐走法，但点击位置错误

**解决方案：**
1. 重新校准棋盘
2. 删除 `calibration.pkl` 文件
3. 精确点击棋盘的边缘
4. 不包含棋盘边框，只包含格子区域

#### 2. pyautogui 权限问题
**症状：** 鼠标完全没有移动

**解决方案：**
1. 以管理员身份运行程序
2. 检查防病毒软件是否阻止
3. 在防火墙中允许Python

#### 3. 屏幕缩放问题
**症状：** 鼠标移动位置偏移

**解决方案：**
1. 在Windows显示设置中禁用缩放
2. 设置缩放为100%
3. 或调整代码中的坐标计算

#### 4. 程序运行太快
**症状：** 点击后立即退出，来不及反应

**解决方案：**
- 已在代码中增加延迟
- 使用动画移动鼠标
- 添加调试信息

## 🧪 测试步骤

### 步骤1：测试鼠标控制

运行鼠标测试脚本：

```bash
cd C:\Users\Administrator\Desktop\chess-bot
python test_mouse.py
```

**观察：**
- 鼠标是否移动
- 点击是否生效
- 移动是否准确

### 步骤2：检查棋盘校准

1. 运行主程序
2. 选择校准
3. 点击棋盘左上角和右下角
4. 查看 `board_preview.png` 是否正确

### 步骤3：手动测试坐标

在程序中，点击位置的坐标应该对应棋盘格子。

**检查：**
- 格子大小是否合理（通常50-100像素）
- 棋盘区域是否正确

## 🔧 调整方法

### 方法1：重新校准

```bash
cd C:\Users\Administrator\Desktop\chess-bot
python screen_recognition_bot.py
```

选择校准，重新点击棋盘边缘。

### 方法2：手动调整坐标

在代码中添加偏移量：

```python
# 添加偏移量（如果点击位置不准确）
offset_x = 10
offset_y = 10

from_x = x1 + from_file * square_w + square_w // 2 + offset_x
from_y = y1 + (7 - from_rank) * square_h + square_h // 2 + offset_y
```

### 方法3：使用更长的延迟

在代码中增加延迟：

```python
# 在 execute_move 函数中
time.sleep(1.0)  # 增加到1秒
```

## 📋 调试信息

### 查看日志输出

程序现在会显示：

```
执行移动: a8b6
点击起点: (123, 456)
点击终点: (234, 567)
当前鼠标位置: (123, 456)
✓ 已点击起点
✓ 已点击终点
✅ 移动执行完成
```

**检查：**
- 坐标是否合理（在屏幕范围内）
- 鼠标位置是否匹配
- 是否显示"执行完成"

## 🛠️ 系统检查

### 检查pyautogui

```bash
python -c "import pyautogui; print(pyautogui.__version__)"
```

**如果失败：**
```bash
pip install --upgrade pyautogui
```

### 检查权限

**尝试以管理员身份运行：**
1. 右键点击命令提示符
2. 选择"以管理员身份运行"
3. 运行程序

### 检查防病毒软件

**临时禁用防病毒软件测试：**
1. 禁用实时保护
2. 运行程序
3. 观察是否正常工作
4. 记得重新启用

## 🎯 快速解决方案

### 如果鼠标完全不移动

1. 运行 `test_mouse.py` 测试
2. 以管理员身份运行
3. 检查防病毒软件
4. 更新 pyautogui

### 如果鼠标移动但点击无效

1. 增加点击延迟
2. 确保棋盘窗口在前台
3. 检查棋盘是否可交互
4. 尝试手动点击测试

### 如果点击位置不准确

1. 重新校准棋盘
2. 调整屏幕缩放为100%
3. 手动添加偏移量
4. 检查棋盘是否被缩放

## 📞 获取帮助

如果以上方法都不行：

1. **收集信息：**
   - 完整的错误信息
   - 鼠标测试结果
   - 棋盘预览图像

2. **检查：**
   - 运行 `simple_test.py`
   - 运行 `test_mouse.py`
   - 查看 `board_preview.png`

---

**现在程序有详细的调试信息，可以帮助定位问题！** 🔍
