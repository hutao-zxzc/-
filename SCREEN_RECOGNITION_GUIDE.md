# 屏幕棋盘识别使用指南

## 📋 概述

`screen_recognition_bot.py` 是一个增强版的国际象棋机器人，能够自动识别屏幕上的棋盘并使用LCZero引擎进行对弈。

## 🚀 快速开始

### 1. 基本使用

```bash
cd C:\Users\Administrator\Desktop\chess-bot
python screen_recognition_bot.py
```

### 2. 校准棋盘

**方法1：手动校准（推荐）**
- 程序会提示点击棋盘左上角和右下角
- 确保棋盘完全可见，不被遮挡

**方法2：自动检测**
- 程序尝试自动从屏幕识别棋盘
- 需要棋盘对比度清晰，光线充足

### 3. 对弈流程

1. **校准棋盘**（首次运行）
2. **在chess.com/lichess.org上打开一局棋**
3. **让对手走一步**
4. **在Python程序中按Enter**
5. **AI识别棋盘并推荐走法**
6. **选择是否自动执行移动**

## 🎮 使用模式

### 模式1：纯识别模式
```
按Enter -> AI识别棋盘 -> 推荐走法 -> 你自己点击
```

### 模式2：自动执行模式
```
按Enter -> AI识别棋盘 -> 推荐走法 -> 自动点击执行
```

## 🔧 配置选项

### 搜索深度
```python
move = bot.calculate_best_move(board, depth=10)
```
- `depth=5`: 快速，适合测试
- `depth=10`: 标准对弈速度
- `depth=15-20`: 深度计算，需要更多时间

### 线程数配置
```python
engine.configure({
    "Threads": 4,  # 根据CPU核心数调整
    "NNCacheSize": 200,
})
```

## 📊 识别准确性

### 提高识别准确性的方法

1. **环境条件**
   - ✅ 棋盘对比度高（深色格子 vs 浅色格子）
   - ✅ 光线充足且均匀
   - ✅ 棋盘完全可见
   - ❌ 阴影、反光、遮挡

2. **棋盘样式**
   - 标准的深浅交替格子
   - 棋子有清晰的轮廓
   - 避免复杂的背景图案

3. **校准精确度**
   - 精确点击棋盘边缘
   - 包含整个棋盘（不包含边框）
   - 测试识别结果并调整

## 🐛 常见问题

### 识别不准确

**问题：** 棋子识别错误

**解决方案：**
1. 使用 `test` 命令查看识别结果
2. 调整棋盘位置和大小
3. 重新校准
4. 手动输入正确的FEN字符串

### 无法检测到棋盘

**问题：** 自动检测失败

**解决方案：**
1. 使用手动校准
2. 检查棋盘是否可见
3. 确保屏幕分辨率足够高
4. 避免棋盘被其他窗口遮挡

### 移动执行错误

**问题：** 鼠标点击位置不对

**解决方案：**
1. 重新校准棋盘位置
2. 检查棋盘区域设置
3. 确保棋盘没有被移动或缩放

## 🎯 高级功能

### 1. FEN字符串输入
```
输入 'fen' -> 粘贴FEN字符串 -> AI分析
```

### 2. 测试识别
```
输入 'test' -> 查看当前识别结果
```

### 3. 重新校准
```
输入 'recalibrate' -> 重新设置棋盘位置
```

## 📝 命令列表

| 命令 | 功能 |
|------|------|
| Enter | 识别棋盘并推荐走法 |
| fen | 手动输入FEN字符串 |
| test | 测试棋盘识别 |
| recalibrate | 重新校准棋盘 |
| quit | 退出程序 |

## 🔍 FEN获取方法

### 从chess.com获取
1. 打开分析面板
2. 点击"Copy FEN"按钮
3. 粘贴到程序中

### 从lichess.org获取
1. 打开分析模式
2. 点击"Copy PGN/FEN"
3. 选择FEN
4. 粘贴到程序中

## ⚠️ 注意事项

### 使用建议
1. 首次使用建议先用测试模式熟悉
2. 重要的对弈建议先看推荐，再决定是否自动执行
3. 定期检查识别准确性
4. 保持良好的网络连接（如果使用在线引擎）

### 安全性
1. 在对弈平台使用AI可能违反服务条款
2. 建议只在练习或友好对局中使用
3. 不要在正式比赛中使用
4. 尊重对手和游戏规则

## 🚀 性能优化

### 提高速度
```python
# 降低搜索深度
move = bot.calculate_best_move(board, depth=5)

# 减少节点数
limit = chess.engine.Limit(nodes=5000)
```

### 提高准确性
```python
# 增加搜索深度
move = bot.calculate_best_move(board, depth=15)

# 增加节点数
limit = chess.engine.Limit(nodes=20000)
```

## 📚 相关文件

- `screen_recognition_bot.py` - 主程序
- `lc0.exe` - LCZero引擎
- `calibration.pkl` - 校准数据（自动生成）
- `lc0_config.txt` - 引擎配置

## 🆘 获取帮助

如果遇到问题：
1. 检查错误信息
2. 查看日志输出
3. 使用测试命令诊断
4. 参考常见问题部分

---

祝你使用愉快！♟️
